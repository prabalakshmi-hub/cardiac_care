name: cardiac_care_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.8

  # Firebase dependencies for authentication and core services
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore:
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/splash_image.png
    - assets/login_background.png

  uses-material-design: true
